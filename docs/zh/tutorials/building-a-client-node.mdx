<Tab title="Node">
  ## 系统要求

  开始之前，请确保你的系统满足以下要求：

  * Mac 或 Windows 计算机
  * 已安装 Node.js 16 或更高版本
  * npm（随 Node.js 一同安装）

  ## 设置开发环境

  首先，创建一个新的 Node.js 项目：

  ```bash
  # 创建项目目录
  mkdir mcp-client
  cd mcp-client

  # 初始化 npm 项目
  npm init -y

  # 安装依赖
  npm install @modelcontextprotocol/sdk @anthropic-ai/sdk dotenv
  npm install -D typescript @types/node

  # 创建 TypeScript 配置
  npx tsc --init

  # 创建所需文件
  mkdir src
  touch src/client.ts
  touch .env
  ```

  将你的 `package.json` 更新以添加必要的配置：

  ```json
  {
    "type": "module",
    "scripts": {
      "build": "tsc",
      "start": "node build/client.js"
    }
  }
  ```

  使用合适的设置更新你的 `tsconfig.json`：

  ```json
  {
    "compilerOptions": {
      "target": "ES2022",
      "module": "Node16",
      "moduleResolution": "Node16",
      "outDir": "./build",
      "rootDir": "./src",
      "strict": true,
      "esModuleInterop": true,
      "skipLibCheck": true,
      "forceConsistentCasingInFileNames": true
    },
    "include": ["src/**/*"]
  }
  ```

  ## 设置你的 API 密钥

  你需要在 [Anthropic Console](https://console.anthropic.com/settings/keys) 获取一把 Anthropic API 密钥。

  创建一个“.env”文件：

  ```bash
  ANTHROPIC_API_KEY=你的密钥
  ```

  将 `.env` 添加到你的 `.gitignore`：

  ```bash
  echo ".env" >> .gitignore
  ```

  ## 创建 MCP 客户端

  首先，让我们设置导入，并在 `src/client.ts` 中创建一个基础的客户端类：

  ```typescript
  import { Client } from "@modelcontextprotocol/sdk/client/index.js";
  import { StdioClientTransport } from "@modelcontextprotocol/sdk/client/stdio.js";
  import Anthropic from "@anthropic-ai/sdk";
  import dotenv from "dotenv";
  import {
    CallToolResultSchema,
    ListToolsResultSchema,
  } from "@modelcontextprotocol/sdk/types.js";
  import * as readline from "node:readline";

  dotenv.config();

  interface MCPClientConfig {
    name?: string;
    version?: string;
  }

  class MCPClient {
    private client: Client | null = null;
    private anthropic: Anthropic;
    private transport: StdioClientTransport | null = null;

    constructor(config: MCPClientConfig = {}) {
      this.anthropic = new Anthropic();
    }

    // 在这里添加方法
  }
  ```

  ## 服务器连接管理

  接下来，实现用于连接 MCP 服务器的方法：

  ```typescript
  async connectToServer(serverScriptPath: string): Promise<void> {
      const isPython = serverScriptPath.endsWith(".py");
      const isJs = serverScriptPath.endsWith(".js");

      if (!isPython && !isJs) {
        throw new Error("服务器脚本必须为 .py 或 .js 文件");
      }

      const command = isPython ? "python" : "node";

      this.transport = new StdioClientTransport({
        command,
        args: [serverScriptPath],
      });

      this.client = new Client(
        {
          name: "mcp-client",
          version: "1.0.0",
        },
        {
          capabilities: {},
        }
      );

      await this.client.connect(this.transport);

      // 列出可用工具
      const response = await this.client.request(
        { method: "tools/list" },
        ListToolsResultSchema
      );

      console.log(
        "\n已连接到服务器，已提供的工具：",
        response.tools.map((tool: any) => tool.name)
      );
    }
  ```

  ## 查询处理逻辑

  现在添加用于处理查询和工具调用的核心功能：

  ```typescript
  async processQuery(query: string): Promise<string> {
      if (!this.client) {
        throw new Error("客户端未连接");
      }

      // 用用户的查询初始化消息数组
      let messages: Anthropic.MessageParam[] = [
        {
          role: "user",
          content: query,
        },
      ];

      // 获取可用的工具
      const toolsResponse = await this.client.request(
        { method: "tools/list" },
        ListToolsResultSchema
      );

      const availableTools = toolsResponse.tools.map((tool: any) => ({
        name: tool.name,
        description: tool.description,
        input_schema: tool.inputSchema,
      }));

      const finalText: string[] = [];
      let currentResponse = await this.anthropic.messages.create({
        model: "claude-3-5-sonnet-20241022",
        max_tokens: 1000,
        messages,
        tools: availableTools,
      });

      // Process the response and any tool calls
      while (true) {
        // 将 Claude 的回复添加到最终文本和消息列表中
        for (const content of currentResponse.content) {
          if (content.type === "text") {
            finalText.push(content.text);
          } else if (content.type === "tool_use") {
            const toolName = content.name;
            const toolArgs = content.input;

            // 执行工具调用
            const result = await this.client.request(
              {
                method: "tools/call",
                params: {
                  name: toolName,
                  arguments: toolArgs,
                },
              },
              CallToolResultSchema
            );

            finalText.push(
              `[调用工具 ${toolName}，参数：${JSON.stringify(toolArgs)}]`
            );

            // 将 Claude 的回复（包含工具使用）加入消息列表
            messages.push({
              role: "assistant",
              content: currentResponse.content,
            });

            // 将工具结果加入消息列表
            messages.push({
              role: "user",
              content: [
                {
                  type: "tool_result",
                  tool_use_id: content.id,
                  content: [
                    { type: "text", text: JSON.stringify(result.content) },
                  ],
                },
              ],
            });

            // 使用工具结果获取 Claude 的下一条回复
            currentResponse = await this.anthropic.messages.create({
              model: "claude-3-5-sonnet-20241022",
              max_tokens: 1000,
              messages,
              tools: availableTools,
            });

            // 将 Claude 对工具结果的解读添加到最终文本中
            if (currentResponse.content[0]?.type === "text") {
              finalText.push(currentResponse.content[0].text);
            }

            // 继续循环以处理其他可能的工具调用
            continue;
          }
        }

        // 如果执行到这里，表示响应中没有工具调用
        break;
      }

      return finalText.join("\n");
    }
  ```

  ## 交互式对话界面

  添加聊天循环与清理功能：

  ```typescript
  async chatLoop(): Promise<void> {
      console.log("\nMCP 客户端已启动！");
      console.log("输入查询内容，或输入 'quit' 退出。");

      // Using Node's readline for console input
      const rl = readline.createInterface({
        input: process.stdin,
        output: process.stdout,
      });

      const askQuestion = () => {
        rl.question("\n查询：", async (query: string) => {
          try {
            if (query.toLowerCase() === "quit") {
              await this.cleanup();
              rl.close();
              return;
            }

            const response = await this.processQuery(query);
            console.log("\n" + response);
            askQuestion();
          } catch (error) {
            console.error("\n错误：", error);
            askQuestion();
          }
        });
      };

      askQuestion();
    }

    async cleanup(): Promise<void> {
      if (this.transport) {
        await this.transport.close();
      }
    }
  ```

  ## 入口点（主）

  最后，在类外部添加主要执行逻辑：

  ```typescript
  // 主执行入口
  async function main() {
    if (process.argv.length < 3) {
      console.log("用法：ts-node client.ts <path_to_server_script>");
      process.exit(1);
    }

    const client = new MCPClient();
    try {
      await client.connectToServer(process.argv[2]);
      await client.chatLoop();
    } catch (error) {
      console.error("错误：", error);
      await client.cleanup();
      process.exit(1);
    }
  }

  // 若为主模块，则运行 main
  if (import.meta.url === new URL(process.argv[1], "file:").href) {
    main();
  }

  export default MCPClient;
  ```

  ## 运行客户端

  要让你的客户端连接并运行于任意 MCP 服务器：

  ```bash
  # 构建 TypeScript 代码。每次更新 `client.ts` 后请务必重新构建！
  npm run build

  # 运行客户端
  node build/client.js path/to/server.py  # 适用于 Python 服务器
  node build/client.js path/to/server.js  # 适用于 Node.js 服务器
  ```

  MCP 客户端将：

  1. 连接到指定服务器
  2. 列出可用工具
  3. 开始一段交互式聊天会话，你可以：
     * 输入查询
     * 查看工具执行情况
     * 接收来自 Claude 的回应

  ## 关键组件详解

  #### 1. 客户端初始化

  * `MCPClient` 类在会话管理与 API 客户端的支持下完成初始化
  * 使用基本功能初始化 MCP 客户端
  * 配置 Anthropic 客户端以进行 Claude 交互

  #### 2. 连接服务器

  * 同时支持 Python 与 Node.js 服务器
  * 验证服务器脚本类型
  * 设置合适的通信渠道
  * 连接后列出可用工具

  #### 3. 查询处理

  * 保留会话上下文
  * 处理 Claude 的响应和工具调用
  * 管理 Claude 与工具之间的消息流转
  * 将结果合并为连贯的答复

  #### 4. 交互界面

  * 提供一个简单的命令行界面
  * 处理用户输入并展示响应
  * 包含基本错误处理
  * 允许优雅地退出

  #### 5. 资源管理

  * 正确释放资源
  * 连接问题的错误处理
  * 优雅停机流程

  ### 常见自定义项

  1. **工具处理**
     * 修改 `processQuery()` 以处理特定类型的工具
     * 为工具调用添加自定义错误处理
     * 实现针对工具的响应格式化

  2. **响应处理**
     * 自定义工具结果的呈现格式
     * 添加响应过滤或转换
     * 实现自定义日志记录

  3. **用户界面**
     * 添加 GUI 或 Web 界面
     * 实现更丰富的控制台输出
     * 添加命令历史或自动补全

  ### 最佳实践

  1. **错误处理**
     * 始终在 try-catch 块中包裹工具调用
     * 提供清晰且有用的错误信息
     * 优雅地处理连接异常

  2. **资源管理**
     * 使用合适的清理方法
     * 完成后关闭连接
     * 处理服务器断连情况

  3. **安全**
     * 将 API 密钥安全存放在 `.env` 中
     * 验证服务器的响应
     * 谨慎处理工具权限

  ### 故障排除

  #### 服务器路径相关问题

  * 请再次确认你的服务器脚本路径
  * 如果相对路径不可用，请改用绝对路径
  * 对于 Windows 用户，请使用正斜杠（/）或转义反斜杠（\）
  * 确认服务器文件使用正确的扩展名（.py 或 .js）

  正确的路径用法示例：

  ```bash
  # 相对路径
  node build/client.js ./server/weather.js

  # 绝对路径
  node build/client.js /Users/username/projects/mcp-server/weather.js

  # Windows 路径（两种格式均可）
  node build/client.js C:/projects/mcp-server/weather.js
  node build/client.js C:\\projects\\mcp-server\\weather.js
  ```

  #### 连接问题

  * 确认服务器脚本是否存在并具有正确权限
  * 检查服务器脚本是否具有可执行权限
  * 确保已安装服务器脚本所需的依赖
  * 尝试直接运行服务器脚本以检查是否有错误

  #### 工具运行问题

  * 检查服务器日志中的错误信息
  * 验证工具输入参数是否符合架构
  * 确保工具依赖项可用
  * 添加调试日志以跟踪执行路径
</Tab>