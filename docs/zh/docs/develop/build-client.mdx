---
title: "构建 MCP 客户端"
description: "开始构建你自己的客户端，以集成所有 MCP 服务器。"
---

在本教程中，你将学习如何构建一个由 LLM 驱动的聊天机器人客户端，并将其连接到 MCP 服务器。

在开始之前，建议先阅读我们的 [构建 MCP 服务器](/zh/docs/develop/build-server) 教程，以便理解客户端与服务器的通信方式。

<Tabs>
  <Tab title="Python">
    [本教程的完整代码可在此处获取。](https://github.com/modelcontextprotocol/quickstart-resources/tree/main/mcp-client-python)

    ## 系统要求

    开始之前，请确保你的系统满足以下要求：

    * Mac 或 Windows 计算机
    * 已安装的最新 Python 版本
    * 已安装 `uv` 的最新版本

    ## 配置你的环境

    首先，使用 `uv` 创建一个新的 Python 项目：

    ```bash
    # 创建项目目录
    uv init mcp-client
    cd mcp-client

    # 创建虚拟环境
    uv venv

    # 激活虚拟环境
    # 在 Windows 上：
    .venv\Scripts\activate
    # 在 Unix 或 macOS 上：
    source .venv/bin/activate

    # 安装所需的包
    uv add mcp anthropic python-dotenv

    # 删除样板文件
    # 在 Windows 上：
    del main.py
    # 在 Unix 或 macOS 上：
    rm main.py

    # 创建主文件
    touch client.py
    ```

    ## 设置您的 API 密钥

    你需要在 [Anthropic Console](https://console.anthropic.com/settings/keys) 获取一把 Anthropic API 密钥。

    创建一个 `.env` 文件以存储它：

    ```bash
    # 创建 .env 文件
    touch .env
    ```

    将你的密钥添加到 `.env` 文件：

    ```bash
    ANTHROPIC_API_KEY=<在此填写你的密钥>
    ```

    将 `.env` 添加到你的 `.gitignore`：

    ```bash
    echo ".env" >> .gitignore
    ```

    <Warning>
      请务必妥善保管你的 `ANTHROPIC_API_KEY`！
    </Warning>

    ## 创建 MCP 客户端

    ### 客户端基础结构

    首先，让我们先导入所需模块，并创建一个基础的客户端类：

    ```python
    import asyncio
    from typing import Optional
    from contextlib import AsyncExitStack

    from mcp import ClientSession, StdioServerParameters
    from mcp.client.stdio import stdio_client

    from anthropic import Anthropic
    from dotenv import load_dotenv

    load_dotenv()  # 从 .env 文件加载环境变量

    class MCPClient:
        def __init__(self):
            # 初始化会话与客户端对象
            self.session: Optional[ClientSession] = None
            self.exit_stack = AsyncExitStack()
            self.anthropic = Anthropic()
        # 后续方法将在此处添加
    ```

    ### 服务器连接管理

    接下来，我们将实现一个用于连接 MCP 服务器的方法：

    ```python
    async def connect_to_server(self, server_script_path: str):
        """连接到 MCP 服务器

        参数：
            server_script_path：服务器脚本路径（.py 或 .js）
        """
        is_python = server_script_path.endswith('.py')
        is_js = server_script_path.endswith('.js')
        if not (is_python or is_js):
            raise ValueError("服务器脚本必须为 .py 或 .js 文件")

        command = "python" if is_python else "node"
        server_params = StdioServerParameters(
            command=command,
            args=[server_script_path],
            env=None
        )

        stdio_transport = await self.exit_stack.enter_async_context(stdio_client(server_params))
        self.stdio, self.write = stdio_transport
        self.session = await self.exit_stack.enter_async_context(ClientSession(self.stdio, self.write))

        await self.session.initialize()

        # 列出可用工具
        response = await self.session.list_tools()
        tools = response.tools
        print("\n已连接到服务器，可用工具：", [tool.name for tool in tools])
    ```

    ### 查询处理逻辑

    现在，让我们添加用于处理查询和工具调用的核心功能：

    ```python
    async def process_query(self, query: str) -> str:
        """使用 Claude 和可用工具处理查询"""
        messages = [
            {
                "role": "user",
                "content": query
            }
        ]

        response = await self.session.list_tools()
        available_tools = [{
            "name": tool.name,
            "description": tool.description,
            "input_schema": tool.inputSchema
        } for tool in response.tools]

        # 首次调用 Claude API
        response = self.anthropic.messages.create(
            model="claude-3-5-sonnet-20241022",
            max_tokens=1000,
            messages=messages,
            tools=available_tools
        )

        # 处理响应并执行工具调用
        final_text = []

        assistant_message_content = []
        for content in response.content:
            if content.type == 'text':
                final_text.append(content.text)
                assistant_message_content.append(content)
            elif content.type == 'tool_use':
                tool_name = content.name
                tool_args = content.input

                # 执行工具调用
                result = await self.session.call_tool(tool_name, tool_args)
                final_text.append(f"[调用工具 {tool_name}，参数 {tool_args}]")

                assistant_message_content.append(content)
                messages.append({
                    "role": "assistant",
                    "content": assistant_message_content
                })
                messages.append({
                    "role": "user",
                    "content": [
                        {
                            "type": "tool_result",
                            "tool_use_id": content.id,
                            "content": result.content
                        }
                    ]
                })

                # 获取 Claude 的下一条响应
                response = self.anthropic.messages.create(
                    model="claude-3-5-sonnet-20241022",
                    max_tokens=1000,
                    messages=messages,
                    tools=available_tools
                )

                final_text.append(response.content[0].text)

        return "\n".join(final_text)
    ```

    ### 交互式聊天界面

    现在我们要添加聊天循环和清理功能：

    ```python
    async def chat_loop(self):
        """运行交互式聊天循环"""
        print("\nMCP 客户端已启动！")
        print("输入查询，或输入 'quit' 退出。")

        while True:
            try:
                query = input("\n查询：").strip()

                if query.lower() == 'quit':
                    break

                response = await self.process_query(query)
                print("\n" + response)

            except Exception as e:
                print(f"\n错误：{str(e)}")

    async def cleanup(self):
        """清理资源"""
        await self.exit_stack.aclose()
    ```

    ### 主入口

    最后，我们来添加主要的执行逻辑：

    ```python
    async def main():
        if len(sys.argv) < 2:
            print("用法：python client.py <服务器脚本路径>")
            sys.exit(1)

        client = MCPClient()
        try:
            await client.connect_to_server(sys.argv[1])
            await client.chat_loop()
        finally:
            await client.cleanup()

    if __name__ == "__main__":
        import sys
        asyncio.run(main())
    ```

    你可以在[此处](https://gist.github.com/zckly/f3f28ea731e096e53b39b47bf0a2d4b1)找到完整的 `client.py` 文件。

    ## 关键组件说明

    ### 1. 初始化客户端

    * `MCPClient` 类在初始化时设置会话管理和 API 客户端
    * 使用 `AsyncExitStack` 实现规范的资源管理
    * 为 Claude 交互配置 Anthropic 客户端

    ### 2. 连接服务器

    * 支持 Python 和 Node.js 两种服务器
    * 校验服务器脚本类型
    * 建立合适的通信通道
    * 初始化会话并列出可用工具

    ### 3. 查询处理

    * 保留对话上下文
    * 处理 Claude 的回复与工具调用
    * 管理 Claude 与工具之间的消息流
    * 将结果整合为连贯的响应

    ### 4. 交互界面

    * 提供简单的命令行界面
    * 处理用户输入并展示响应
    * 包含基础错误处理
    * 支持优雅退出

    ### 5. 资源管理

    * 正确释放资源
    * 连接问题处理与错误恢复
    * 优雅关闭流程

    ## 常见自定义项

    1. **工具使用**
       * 修改 `process_query()` 以处理特定的工具类型
       * 为工具调用添加自定义错误处理
       * 实现面向工具的响应格式化

    2. **响应处理**
       * 自定义工具结果的呈现格式
       * 添加响应过滤或转换
       * 实现自定义日志

    3. **用户界面**
       * 添加 GUI 或 Web 界面
       * 实现更丰富的控制台输出
       * 添加命令历史或自动补全

    ## 运行 MCP 客户端

    要在任意 MCP 服务器上运行你的客户端：

    ```bash
    uv run client.py path/to/server.py # Python 服务器
    uv run client.py path/to/build/index.js # Node.js 服务器
    ```

    <Note>
      如果你是从服务器快速入门继续进行天气教程，你的命令可能类似这样：`python client.py .../quickstart-resources/weather-server-python/weather.py`
    </Note>

    MCP 客户端将：

    1. 连接到指定服务器
    2. 列出可用工具
    3. 开始一段交互式聊天会话，你可以：
       * 输入查询
       * 查看工具执行
       * 接收来自 Claude 的回复

    如果按服务器快速入门中的步骤连接到天气服务器，效果应大致如下：

    <Frame>
      <img src="/images/client-claude-cli-python.png" />
    </Frame>

    ## 原理说明

    当你提交请求时：

    1. 客户端从服务器获取可用的工具列表
    2. 你的查询会连同工具描述一起发送给 Claude
    3. Claude 决定是否以及使用哪些工具
    4. 客户端通过服务器执行任何请求的工具调用
    5. 结果会返回给 Claude
    6. Claude 提供自然语言回复
    7. 会向你显示响应

    ## 最佳实践

    1. **错误处理**
       * 始终将工具调用置于 try-catch 块中
       * 提供有意义的错误信息
       * 优雅地处理连接故障

    2. **资源管理**
       * 使用 `AsyncExitStack` 进行正确的清理
       * 任务完成后关闭连接
       * 处理服务器断开连接的情况

    3. **安全性**
       * 将 API 密钥安全地存放在 `.env` 中
       * 校验服务器响应
       * 谨慎授予工具权限

    ## 故障排查

    ### 服务器路径相关问题

    * 请再次核对服务器脚本的路径是否正确
    * 如果相对路径不可用，请使用绝对路径
    * 对于 Windows 用户，请确保在路径中使用正斜杠（/）或转义的反斜杠（\）
    * 确认服务器文件使用正确的扩展名（Python 为 .py，Node.js 为 .js）

    正确的路径使用示例：

    ```bash
    # 相对路径
    uv run client.py ./server/weather.py

    # 绝对路径
    uv run client.py /Users/username/projects/mcp-server/weather.py

    # Windows 路径（两种格式都支持）
    uv run client.py C:/projects/mcp-server/weather.py
    uv run client.py C:\\projects\\mcp-server\\weather.py
    ```

    ### 响应时机

    * 首次响应可能需要最多 30 秒
    * 这是正常现象，通常会在以下情况下出现：
      * 服务器开始初始化
      * Claude 开始处理查询
      * 正在执行工具
    * 后续的响应通常会更快
    * 请勿在初始等待期间中断此过程

    ### 常见错误消息

    如果你看到：

    * `FileNotFoundError`：请检查服务器路径
    * `Connection refused`：请确保服务器正在运行且路径正确
    * `Tool execution failed`：请确认该工具所需的环境变量已正确设置
    * `Timeout error`：请考虑在客户端配置中增加超时阈值
  </Tab>

  <Tab title="Node">
    [你可以在此处找到本教程的完整代码。](https://github.com/modelcontextprotocol/quickstart-resources/tree/main/mcp-client-typescript)

    ## 系统要求

    在开始之前，请确保你的系统符合以下要求：

    * Mac 或 Windows 计算机
    * 已安装 Node.js 17 或更高版本
    * 已安装最新版的 `npm`
    * Anthropic API 密钥（Claude）

    ## 设置开发环境

    首先，让我们创建并配置项目：

    <CodeGroup>
      ```bash macOS/Linux
      # 创建项目目录
      mkdir mcp-client-typescript
      cd mcp-client-typescript

      # 初始化 npm 项目
      npm init -y

      # 安装依赖项
      npm install @anthropic-ai/sdk @modelcontextprotocol/sdk dotenv

      # 安装开发依赖项
      npm install -D @types/node typescript

      # 创建源文件
      touch index.ts
      ```

      ```powershell Windows
      # 创建项目目录
      md mcp-client-typescript
      cd mcp-client-typescript

      # 初始化 npm 项目
      npm init -y

      # 安装依赖项
      npm install @anthropic-ai/sdk @modelcontextprotocol/sdk dotenv

      # 安装开发依赖项
      npm install -D @types/node typescript

      # 创建源文件
      new-item index.ts
      ```
    </CodeGroup>

    在 `package.json` 中设置 `type: "module"` 并添加构建脚本：

    ```json package.json
    {
      "type": "module",
      "scripts": {
        "build": "tsc && chmod 755 build/index.js"
      }
    }
    ```

    在项目根目录创建一个 `tsconfig.json`：

    ```json tsconfig.json
    {
      "compilerOptions": {
        "target": "ES2022",
        "module": "Node16",
        "moduleResolution": "Node16",
        "outDir": "./build",
        "rootDir": "./",
        "strict": true,
        "esModuleInterop": true,
        "skipLibCheck": true,
        "forceConsistentCasingInFileNames": true
      },
      "include": ["index.ts"],
      "exclude": ["node_modules"]
    }
    ```

    ## 配置您的 API 密钥

    你需要从 [Anthropic Console](https://console.anthropic.com/settings/keys) 获取一把 Anthropic API 密钥。

    创建一个 `.env` 文件进行保存：

    ```bash
    echo "ANTHROPIC_API_KEY=<在此填写你的密钥>" > .env
    ```

    将 `.env` 添加到你的 `.gitignore`：

    ```bash
    echo ".env" >> .gitignore
    ```

    <Warning>
      请务必妥善保管你的 `ANTHROPIC_API_KEY`！
    </Warning>

    ## 创建 MCP 客户端

    ### 客户端基础结构

    首先，在 `index.ts` 中先导入依赖，然后创建一个基础的客户端类：

    ```typescript
    import { Anthropic } from "@anthropic-ai/sdk";
    import {
      MessageParam,
      Tool,
    } from "@anthropic-ai/sdk/resources/messages/messages.mjs";
    import { Client } from "@modelcontextprotocol/sdk/client/index.js";
    import { StdioClientTransport } from "@modelcontextprotocol/sdk/client/stdio.js";
    import readline from "readline/promises";
    import dotenv from "dotenv";

    dotenv.config();

    const ANTHROPIC_API_KEY = process.env.ANTHROPIC_API_KEY;
    if (!ANTHROPIC_API_KEY) {
      throw new Error("未设置 ANTHROPIC_API_KEY");
    }

    class MCPClient {
      private mcp: Client;
      private anthropic: Anthropic;
      private transport: StdioClientTransport | null = null;
      private tools: Tool[] = [];

      constructor() {
        this.anthropic = new Anthropic({
          apiKey: ANTHROPIC_API_KEY,
        });
        this.mcp = new Client({ name: "mcp-client-cli", version: "1.0.0" });
      }
      // 方法将在这里编写
    }
    ```

    ### 服务器连接管理

    接下来，我们将实现与 MCP 服务器的连接方法：

    ```typescript
    async connectToServer(serverScriptPath: string) {
      try {
        const isJs = serverScriptPath.endsWith(".js");
        const isPy = serverScriptPath.endsWith(".py");
        if (!isJs && !isPy) {
          throw new Error("服务器脚本必须为 .js 或 .py 文件");
        }
        const command = isPy
          ? process.platform === "win32"
            ? "python"
            : "python3"
          : process.execPath;

        this.transport = new StdioClientTransport({
          command,
          args: [serverScriptPath],
        });
        await this.mcp.connect(this.transport);

        const toolsResult = await this.mcp.listTools();
        this.tools = toolsResult.tools.map((tool) => {
          return {
            name: tool.name,
            description: tool.description,
            input_schema: tool.inputSchema,
          };
        });
        console.log(
          "已连接到服务器，已加载的工具：",
          this.tools.map(({ name }) => name)
        );
      } catch (e) {
        console.log("连接 MCP 服务器失败：", e);
        throw e;
      }
    }
    ```

    ### 查询处理逻辑

    现在让我们添加用于处理查询和工具调用的核心功能：

    ```typescript
    async processQuery(query: string) {
      const messages: MessageParam[] = [
        {
          role: "user",
          content: query,
        },
      ];

      const response = await this.anthropic.messages.create({
        model: "claude-3-5-sonnet-20241022",
        max_tokens: 1000,
        messages,
        tools: this.tools,
      });

      const finalText = [];

      for (const content of response.content) {
        if (content.type === "text") {
          finalText.push(content.text);
        } else if (content.type === "tool_use") {
          const toolName = content.name;
          const toolArgs = content.input as { [x: string]: unknown } | undefined;

          const result = await this.mcp.callTool({
            name: toolName,
            arguments: toolArgs,
          });
          finalText.push(
            `[正在调用工具 ${toolName}，参数：${JSON.stringify(toolArgs)}]`
          );

          messages.push({
            role: "user",
            content: result.content as string,
          });

          const response = await this.anthropic.messages.create({
            model: "claude-3-5-sonnet-20241022",
            max_tokens: 1000,
            messages,
          });

          finalText.push(
            response.content[0].type === "text" ? response.content[0].text : ""
          );
        }
      }

      return finalText.join("\n");
    }
    ```

    ### 交互式对话界面

    现在我们来添加聊天循环和清理功能：

    ```typescript
    async chatLoop() {
      const rl = readline.createInterface({
        input: process.stdin,
        output: process.stdout,
      });

      try {
        console.log("\nMCP 客户端已启动！");
        console.log("输入查询内容，或输入“quit”退出。");

        while (true) {
          const message = await rl.question("\n查询：");
          if (message.toLowerCase() === "quit") {
            break;
          }
          const response = await this.processQuery(message);
          console.log("\n" + response);
        }
      } finally {
        rl.close();
      }
    }

    async cleanup() {
      await this.mcp.close();
    }
    ```

    ### 主要入口

    最后，我们添加主执行逻辑：

    ```typescript
    async function main() {
      if (process.argv.length < 3) {
        console.log("用法：node index.ts <服务器脚本路径>");
        return;
      }
      const mcpClient = new MCPClient();
      try {
        await mcpClient.connectToServer(process.argv[2]);
        await mcpClient.chatLoop();
      } finally {
        await mcpClient.cleanup();
        process.exit(0);
      }
    }

    main();
    ```

    ## 运行 MCP 客户端

    要让你的客户端连接并运行于任意 MCP 服务器：

    ```bash
    # 构建 TypeScript
    npm run build

    # 运行客户端
    node build/index.js path/to/server.py # Python 服务器
    node build/index.js path/to/build/index.js # Node 服务器
    ```

    <Note>
      如果你在延续服务器快速入门中的天气教程，你的命令可能如下所示：`node build/index.js .../quickstart-resources/weather-server-typescript/build/index.js`
    </Note>

    **MCP 客户端将：**

    1. 连接指定服务器
    2. 列出可用工具
    3. 开始一场交互式聊天会话，你可以：
       * 输入查询
       * 查看工具执行情况
       * 接收 Claude 的回复

    ## 工作机制

    当你提交请求时：

    1. 客户端从服务器获取可用工具列表
    2. 你的查询会连同工具描述一起发送给 Claude
    3. Claude 决定是否使用工具（以及使用哪些工具）
    4. 客户端通过服务器执行所有请求的工具调用
    5. 结果会发送回 Claude
    6. Claude 返回自然语言响应
    7. 响应会显示给你

    ## 最佳实践

    1. **异常处理**
       * 使用 TypeScript 的类型系统以更好地检测错误
       * 将工具调用包裹在 try-catch 块中
       * 提供清晰有用的错误信息
       * 以稳健方式处理连接问题

    2. **安全**
       * 将 API 密钥安全地存放在 `.env` 中
       * 验证服务器的响应
       * 谨慎管理工具权限

    ## 疑难解答

    ### 服务器路径相关问题

    * 请再次确认服务器脚本的路径是否正确
    * 如果相对路径无法正常工作，请使用绝对路径
    * 对于 Windows 用户，请确保在路径中使用正斜杠（/）或转义反斜杠（\）
    * 验证服务器文件的扩展名是否正确（Node.js 为 .js，Python 为 .py）

    正确的路径使用示例：

    ```bash
    # 相对路径
    node build/index.js ./server/build/index.js

    # 绝对路径
    node build/index.js /Users/username/projects/mcp-server/build/index.js

    # Windows 路径（两种格式均可）
    node build/index.js C:/projects/mcp-server/build/index.js
    node build/index.js C:\\projects\\mcp-server\\build\\index.js
    ```

    ### 响应时机

    * 首次响应可能需要长达 30 秒才会返回
    * 这是正常现象，通常会在以下情况下发生：
      * 服务器开始初始化
      * Claude 开始处理查询
      * 开始执行工具
    * 后续的响应通常会更快
    * 在初始等待期间请勿中断该流程

    ### 常见错误信息

    如果你看到：

    * `Error: Cannot find module`：检查你的构建目录，并确保已成功完成 TypeScript 编译
    * `Connection refused`：请确保服务器正在运行且路径正确
    * `工具执行失败`：请检查该工具所需的环境变量是否已正确设置
    * `ANTHROPIC_API_KEY is not set`：请检查你的 .env 文件和环境变量
    * `TypeError`：请确保你为工具参数使用正确的类型
  </Tab>

  <Tab title="Java">
    <Note>
      这是一个基于 Spring AI MCP 自动配置和 Boot 启动器的快速入门示例。
      要了解如何手动创建同步和异步的 MCP 客户端，请参阅 [Java SDK Client](/zh/sdk/java/mcp-client) 文档。
    </Note>

    此示例演示如何构建一个交互式聊天机器人，将 Spring AI 的模型上下文协议（MCP）与 [Brave Search MCP Server](https://github.com/modelcontextprotocol/servers-archived/tree/main/src/brave-search) 相结合。该应用提供由 Anthropic 的 Claude 模型驱动的对话界面，可通过 Brave Search 进行互联网搜索，实现以自然语言与实时网页数据交互。
    [你可以在此处查看本教程的完整代码。](https://github.com/spring-projects/spring-ai-examples/tree/main/model-context-protocol/web-search/brave-chatbot)

    ## 系统要求

    开始之前，请确保你的系统满足以下要求：

    * Java 17 或更高版本
    * Maven 3.6 及以上
    * npx 包执行器
    * Anthropic API 密钥（Claude）
    * Brave Search API 密钥

    ## 配置您的环境

    1. 安装 npx（Node Package eXecute）：
       首先，确保已安装 [npm](https://docs.npmjs.com/downloading-and-installing-node-js-and-npm)，
       然后运行：

       ```bash
       npm install -g npx
       ```

    2. 克隆该仓库：

       ```bash
       git clone https://github.com/spring-projects/spring-ai-examples.git
       cd model-context-protocol/brave-chatbot
       ```

    3. 设置你的 API 密钥：

       ```bash
       export ANTHROPIC_API_KEY='your-anthropic-api-key-here'
       export BRAVE_API_KEY='your-brave-api-key-here'
       ```

    4. 构建应用程序：

       ```bash
       ./mvnw clean install
       ```

    5. 使用 Maven 运行应用：
       ```bash
       ./mvnw spring-boot:run
       ```

    <Warning>
      请务必妥善保管你的 `ANTHROPIC_API_KEY` 和 `BRAVE_API_KEY` 密钥！
    </Warning>

    ## 运行机制

    该应用通过多个组件将 Spring AI 与 Brave Search 的 MCP 服务器进行集成：

    ### MCP 客户端配置

    1. pom.xml 中的必需依赖：

    ```xml
    <dependency>
        <groupId>org.springframework.ai</groupId>
        <artifactId>spring-ai-starter-mcp-client</artifactId>
    </dependency>
    <dependency>
        <groupId>org.springframework.ai</groupId>
        <artifactId>spring-ai-starter-model-anthropic</artifactId>
    </dependency>
    ```

    2. 应用属性（application.yml）：

    ```yml
    spring:
      ai:
        mcp:
          client:
            enabled: true
            name: brave-search-client
            version: 1.0.0
            type: SYNC
            request-timeout: 20s
            stdio:
              root-change-notification: true
              servers-configuration: classpath:/mcp-servers-config.json
            toolcallback:
              enabled: true
        anthropic:
          api-key: ${ANTHROPIC_API_KEY}
    ```

    这将激活 `spring-ai-starter-mcp-client`，并根据提供的服务器配置创建一个或多个 `McpClient`。
    将属性 `spring.ai.mcp.client.toolcallback.enabled=true` 设为 true 可启用工具回调机制，该机制会将所有 MCP 工具自动注册为 Spring AI 工具。
    默认情况下该功能是禁用的。

    3. MCP 服务器配置（`mcp-servers-config.json`）：

    ```json
    {
      "mcpServers": {
        "brave-search": {
          "command": "npx",
          "args": ["-y", "@modelcontextprotocol/server-brave-search"],
          "env": {
            "BRAVE_API_KEY": "<在此输入你的 BRAVE API 密钥>"
          }
        }
      }
    }
    ```

    ### 聊天功能实现

    该聊天机器人使用 Spring AI 的 ChatClient 实现，并集成 MCP 工具：

    ```java
    var chatClient = chatClientBuilder
        .defaultSystem("你是一名实用的助手，精通 AI 和 Java。")
        .defaultToolCallbacks((Object[]) mcpToolAdapter.toolCallbacks())
        .defaultAdvisors(new MessageChatMemoryAdvisor(new InMemoryChatMemory()))
        .build();
    ```

    主要功能：

    * 使用 Claude AI 模型进行自然语言理解
    * 通过 MCP 集成 Brave 搜索，提供实时网页搜索能力
    * 使用 InMemoryChatMemory 维护对话记忆
    * 以交互式命令行应用运行

    ### 构建与运行

    ```bash
    ./mvnw clean install
    java -jar ./target/ai-mcp-brave-chatbot-0.0.1-SNAPSHOT.jar
    ```

    或

    ```bash
    ./mvnw spring-boot:run
    ```

    该应用将启动交互式聊天会话，您可以在其中提问。聊天机器人在需要从互联网获取信息以回答您的问题时，会使用 Brave Search。

    聊天机器人可以：

    * 利用其内置知识回答问题
    * 在需要时使用 Brave Search 进行网页搜索
    * 记住对话中先前消息的上下文
    * 结合多个来源的信息，提供更全面的答案

    ### 高级配置

    MCP 客户端还支持其他配置选项：

    * 通过 `McpSyncClientCustomizer` 或 `McpAsyncClientCustomizer` 定制客户端
    * 多个客户端与多种传输方式：`STDIO` 和 `SSE`（服务器发送事件）
    * 与 Spring AI 工具执行框架的集成
    * 自动化 MCP 客户端的初始化与生命周期管理

    对于基于 WebFlux 的应用程序，可以改用 WebFlux 起步依赖：

    ```xml
    <dependency>
        <groupId>org.springframework.ai</groupId>
        <artifactId>spring-ai-mcp-client-webflux-spring-boot-starter</artifactId>
    </dependency>
    ```

    其功能类似，但采用基于 WebFlux 的服务器发送事件（SSE）传输实现，建议用于生产环境部署。
  </Tab>

  <Tab title="Kotlin">
    [本教程的完整代码可在此处查看。](https://github.com/modelcontextprotocol/kotlin-sdk/tree/main/samples/kotlin-mcp-client)

    ## 系统需求

    开始之前，请确保你的系统满足以下要求：

    * Java 17 或更高版本
    * Anthropic API 密钥（Claude）

    ## 设置开发环境

    首先，如果你尚未安装，先安装 `java` 和 `gradle`。
    你可以从 [Oracle 官方 JDK 网站](https://www.oracle.com/java/technologies/downloads/) 下载 `java`。
    验证你的 `java` 安装：

    ```bash
    java --version
    ```

    现在，让我们创建并配置你的项目：

    <CodeGroup>
      ```bash macOS/Linux
      # 为项目创建新目录
      mkdir kotlin-mcp-client
      cd kotlin-mcp-client

      # 初始化新的 Kotlin 项目
      gradle init
      ```

      ```powershell Windows
      # 为项目创建新目录
      md kotlin-mcp-client
      cd kotlin-mcp-client
      # 初始化新的 Kotlin 项目
      gradle init
      ```
    </CodeGroup>

    运行 `gradle init` 后，你会看到用于创建项目的选项。
    选择 **Application** 作为项目类型，**Kotlin** 作为编程语言，**Java 17** 作为 Java 版本。

    或者，你可以使用 [IntelliJ IDEA 项目向导](https://kotlinlang.org/docs/jvm-get-started.html) 来创建一个 Kotlin 应用。

    创建项目后，添加以下依赖项：

    <CodeGroup>
      ```kotlin build.gradle.kts
      val mcpVersion = "0.4.0"
      val slf4jVersion = "2.0.9"
      val anthropicVersion = "0.8.0"

      dependencies {
          implementation("io.modelcontextprotocol:kotlin-sdk:$mcpVersion")
          implementation("org.slf4j:slf4j-nop:$slf4jVersion")
          implementation("com.anthropic:anthropic-java:$anthropicVersion")
      }
      ```

      ```groovy build.gradle
      def mcpVersion = '0.3.0'
      def slf4jVersion = '2.0.9'
      def anthropicVersion = '0.8.0'
      dependencies {
          implementation "io.modelcontextprotocol:kotlin-sdk:$mcpVersion"
          implementation "org.slf4j:slf4j-nop:$slf4jVersion"
          implementation "com.anthropic:anthropic-java:$anthropicVersion"
      }
      ```
    </CodeGroup>

    另外，将以下插件添加到你的构建脚本中：

    <CodeGroup>
      ```kotlin build.gradle.kts
      plugins {
          id("com.github.johnrengelman.shadow") version "8.1.1"
      }
      ```

      ```groovy build.gradle
      plugins {
          id 'com.github.johnrengelman.shadow' version '8.1.1'
      }
      ```
    </CodeGroup>

    ## 设置您的 API 密钥

    你需要在 [Anthropic Console](https://console.anthropic.com/settings/keys) 获取一个 Anthropic API 密钥。

    设置你的 API Key：

    ```bash
    export ANTHROPIC_API_KEY='你的 Anthropic API 密钥'
    ```

    <Warning>
      务必妥善保管你的 `ANTHROPIC_API_KEY`！
    </Warning>

    ## 创建客户端

    ### 基本客户端结构

    首先，我们来创建一个基本的客户端类：

    ```kotlin
    class MCPClient : AutoCloseable {
        private val anthropic = AnthropicOkHttpClient.fromEnv()
        private val mcp: Client = Client(clientInfo = Implementation(name = "mcp-client-cli", version = "1.0.0"))
        private lateinit var tools: List<ToolUnion>

        // 方法会在这里编写

        override fun close() {
            runBlocking {
                mcp.close()
                anthropic.close()
            }
        }
    ```

    ### 服务器连接管理

    接下来，我们将实现与 MCP 服务器建立连接的方法：

    ```kotlin
    suspend fun connectToServer(serverScriptPath: String) {
        try {
            val command = buildList {
                when (serverScriptPath.substringAfterLast(".")) {
                    "js" -> add("node")
                    "py" -> add(if (System.getProperty("os.name").lowercase().contains("win")) "python" else "python3")
                    "jar" -> addAll(listOf("java", "-jar"))
                    else -> throw IllegalArgumentException("服务器脚本必须是 .js、.py 或 .jar 文件")
                }
                add(serverScriptPath)
            }

            val process = ProcessBuilder(command).start()
            val transport = StdioClientTransport(
                input = process.inputStream.asSource().buffered(),
                output = process.outputStream.asSink().buffered()
            )

            mcp.connect(transport)

            val toolsResult = mcp.listTools()
            tools = toolsResult?.tools?.map { tool ->
                ToolUnion.ofTool(
                    Tool.builder()
                        .name(tool.name)
                        .description(tool.description ?: "")
                        .inputSchema(
                            Tool.InputSchema.builder()
                                .type(JsonValue.from(tool.inputSchema.type))
                                .properties(tool.inputSchema.properties.toJsonValue())
                                .putAdditionalProperty("required", JsonValue.from(tool.inputSchema.required))
                                .build()
                        )
                        .build()
                )
            } ?: emptyList()
            println("已连接到服务器，已加载的工具：${tools.joinToString(", ") { it.tool().get().name() }}")
        } catch (e: Exception) {
            println("连接 MCP 服务器失败：$e")
            throw e
        }
    }
    ```

    同时为 Anthropic 创建一个辅助函数，用于将 `JsonObject` 转换为 `JsonValue`：

    ```kotlin
    private fun JsonObject.toJsonValue(): JsonValue {
        val mapper = ObjectMapper()
        val node = mapper.readTree(this.toString())
        return JsonValue.fromJsonNode(node)
    }
    ```

    ### 查询处理逻辑

    现在让我们添加用于处理查询和工具调用的核心功能：

    ```kotlin
    private val messageParamsBuilder: MessageCreateParams.Builder = MessageCreateParams.builder()
        .model(Model.CLAUDE_3_5_SONNET_20241022)
        .maxTokens(1024)

    suspend fun processQuery(query: String): String {
        val messages = mutableListOf(
            MessageParam.builder()
                .role(MessageParam.Role.USER)
                .content(query)
                .build()
        )

        val response = anthropic.messages().create(
            messageParamsBuilder
                .messages(messages)
                .tools(tools)
                .build()
        )

        val finalText = mutableListOf<String>()
        response.content().forEach { content ->
            when {
                content.isText() -> finalText.add(content.text().getOrNull()?.text() ?: "")

                content.isToolUse() -> {
                    val toolName = content.toolUse().get().name()
                    val toolArgs =
                        content.toolUse().get()._input().convert(object : TypeReference<Map<String, JsonValue>>() {})

                    val result = mcp.callTool(
                        name = toolName,
                        arguments = toolArgs ?: emptyMap()
                    )
                    finalText.add("[正在调用工具 $toolName，参数：$toolArgs]")

                    messages.add(
                        MessageParam.builder()
                            .role(MessageParam.Role.USER)
                            .content(
                                """
                                    "type": "tool_result",
                                    "tool_name": $toolName,
                                    "result": ${result?.content?.joinToString("\n") { (it as TextContent).text ?: "" }}
                                """.trimIndent()
                            )
                            .build()
                    )

                    val aiResponse = anthropic.messages().create(
                        messageParamsBuilder
                            .messages(messages)
                            .build()
                    )

                    finalText.add(aiResponse.content().first().text().getOrNull()?.text() ?: "")
                }
            }
        }

        return finalText.joinToString("\n", prefix = "", postfix = "")
    }
    ```

    ### 交互式对话

    我们将添加对话循环：

    ```kotlin
    suspend fun chatLoop() {
        println("\nMCP 客户端已启动！")
        println("输入查询内容，或输入“quit”退出。")

        while (true) {
            print("\n查询：")
            val message = readLine() ?: break
            if (message.lowercase() == "quit") break
            val response = processQuery(message)
            println("\n$response")
        }
    }
    ```

    ### 主要入口点

    最后，添加主执行函数：

    ```kotlin
    fun main(args: Array<String>) = runBlocking {
        if (args.isEmpty()) throw IllegalArgumentException("用法：java -jar <your_path>/build/libs/kotlin-mcp-client-0.1.0-all.jar <path_to_server_script>")
        val serverPath = args.first()
        val client = MCPClient()
        client.use {
            client.connectToServer(serverPath)
            client.chatLoop()
        }
    }
    ```

    ## 运行 MCP 客户端

    使用任意 MCP 服务器运行你的客户端：

    ```bash
    ./gradlew build

    # 运行客户端
    java -jar build/libs/<your-jar-name>.jar path/to/server.jar # JVM 服务器
    java -jar build/libs/<your-jar-name>.jar path/to/server.py # Python 服务器
    java -jar build/libs/<your-jar-name>.jar path/to/build/index.js # Node 服务器
    ```

    <Note>
      如果你是从服务器快速入门继续进行天气教程，你的命令可能如下：`java -jar build/libs/kotlin-mcp-client-0.1.0-all.jar .../samples/weather-stdio-server/build/libs/weather-stdio-server-0.1.0-all.jar`
    </Note>

    **MCP 客户端将：**

    1. 连接到指定服务器
    2. 列出可用工具
    3. 开始一个交互式聊天会话，你可以在其中：
       * 输入查询
       * 查看工具执行情况
       * 接收来自 Claude 的响应

    ## 工作机制

    这是一个高层次的工作流示意图：

    ```mermaid
    ---
    config:
        theme: neutral
    ---
    sequenceDiagram
        actor User
        participant Client
        participant Claude
        participant MCP_Server as MCP 服务器
        participant 工具

        User->>Client: 发送请求
        Client<<->>MCP_Server: 获取可用工具
        Client->>Claude: 连同工具描述发送请求
        Claude-->>Client: 决定是否调用工具
        Client->>MCP_Server: 请求调用工具
        MCP_Server->>工具: 执行所选工具
        工具-->>MCP_Server: 返回结果
        MCP_Server-->>Client: 返回结果
        Client->>Claude: 发送工具结果
        Claude-->>Client: 提供最终响应
        Client-->>User: 展示响应
    ```

    当你提交查询时：

    1. 客户端从服务器获取可用工具列表
    2. 你的查询会连同工具描述一起发送给 Claude
    3. Claude 决定是否以及使用哪些工具
    4. 客户端通过服务器执行任何请求的工具调用
    5. 结果将返回给 Claude
    6. Claude 使用自然语言进行响应
    7. 响应会显示给你

    ## 最佳实践

    1. **错误处理**
       * 利用 Kotlin 的类型系统显式建模错误
       * 在可能抛出异常时，将外部工具和 API 调用包裹在 `try-catch` 块中
       * 提供清晰、具可读性的错误消息
       * 优雅地处理网络超时与连接问题

    2. **安全**
       * 将 API 密钥和敏感信息安全存储在 `local.properties`、环境变量或秘密管理器中
       * 验证所有外部响应，避免出现意外或不安全的数据使用
       * 在使用工具时谨慎处理权限与信任边界

    ## 故障排查

    ### 服务器路径相关问题

    * 请再次确认你的服务器脚本路径是否正确
    * 如果相对路径无法使用，请改用绝对路径
    * 对于 Windows 用户，请确保在路径中使用正斜杠（/）或转义反斜杠（\）
    * 请确保已安装所需的运行时（Java 使用 Java、Node.js 使用 npm，Python 使用 uv）
    * 确认服务器文件的扩展名是否正确（Java 为 .jar，Node.js 为 .js，Python 为 .py）

    正确的路径使用示例：

    ```bash
    # 相对路径
    java -jar build/libs/client.jar ./server/build/libs/server.jar

    # 绝对路径
    java -jar build/libs/client.jar /Users/username/projects/mcp-server/build/libs/server.jar

    # Windows 路径（两种格式均适用）
    java -jar build/libs/client.jar C:/projects/mcp-server/build/libs/server.jar
    java -jar build/libs/client.jar C:\\projects\\mcp-server\\build\\libs\\server.jar
    ```

    ### 响应时机

    * 首次响应可能需要最多 30 秒才能返回
    * 这是正常现象，通常会在以下情况下出现：
      * 服务器正在初始化
      * Claude 正在处理请求
      * 工具正在运行
    * 后续响应通常会更快
    * 请勿在初始等待期间中断该流程

    ### 常见错误信息

    如果你看到：

    * `Connection refused`：请确认服务器已启动且路径无误
    * `Tool execution failed`：请确认该工具所需的环境变量已正确设置
    * `ANTHROPIC_API_KEY is not set`：请检查你的环境变量
  </Tab>

  <Tab title="C#">
    [本教程的完整代码在此处查看。](https://github.com/modelcontextprotocol/csharp-sdk/tree/main/samples/QuickstartClient)

    ## 系统要求

    开始之前，请确保你的系统满足以下要求：

    * .NET 8.0 或更高版本
    * Anthropic API 密钥（Claude）
    * Windows、Linux 或 macOS

    ## 设置开发环境

    首先，创建一个新的 .NET 项目：

    ```bash
    dotnet new console -n QuickstartClient
    cd QuickstartClient
    ```

    然后，将必需的依赖项添加到你的项目中：

    ```bash
    dotnet add package ModelContextProtocol --prerelease
    dotnet add package Anthropic.SDK
    dotnet add package Microsoft.Extensions.Hosting
    dotnet add package Microsoft.Extensions.AI
    ```

    ## 设置您的 API 密钥

    你需要在 [Anthropic Console](https://console.anthropic.com/settings/keys) 获取一把 Anthropic API 密钥。

    ```bash
    dotnet user-secrets init
    dotnet user-secrets set "ANTHROPIC_API_KEY" "<在此填写你的密钥>"
    ```

    ## 创建客户端

    ### 客户端基础结构

    首先，在 `Program.cs` 文件中设置基本的客户端类：

    ```csharp
    using Anthropic.SDK;
    using Microsoft.Extensions.AI;
    using Microsoft.Extensions.Configuration;
    using Microsoft.Extensions.Hosting;
    using ModelContextProtocol.Client;
    using ModelContextProtocol.Protocol.Transport;

    var builder = Host.CreateApplicationBuilder(args);

    builder.Configuration
        .AddEnvironmentVariables()
        .AddUserSecrets<Program>();
    ```

    这将创建一个 .NET 控制台应用程序的雏形，能够从用户机密中读取 API 密钥。

    接下来，我们将配置 MCP 客户端：

    ```csharp
    var (command, arguments) = GetCommandAndArguments(args);

    var clientTransport = new StdioClientTransport(new()
    {
        Name = "演示服务器",
        Command = command,
        Arguments = arguments,
    });

    await using var mcpClient = await McpClientFactory.CreateAsync(clientTransport);

    var tools = await mcpClient.ListToolsAsync();
    foreach (var tool in tools)
    {
        Console.WriteLine($"已连接到包含以下工具的服务器：{tool.Name}");
    }
    ```

    在 `Program.cs` 文件末尾添加此函数：

    ```csharp
    static (string command, string[] arguments) GetCommandAndArguments(string[] args)
    {
        return args switch
        {
            [var script] when script.EndsWith(".py") => ("python", args),
            [var script] when script.EndsWith(".js") => ("node", args),
            [var script] when Directory.Exists(script) || (File.Exists(script) && script.EndsWith(".csproj")) => ("dotnet", ["run", "--project", script, "--no-build"]),
            _ => throw new NotSupportedException("不支持所提供的服务器脚本。支持的脚本类型为 .py、.js 或 .csproj")
        };
    }
    ```

    这将创建一个 MCP 客户端，并连接到通过命令行参数指定的服务器。随后，它会列出该服务器提供的可用工具。

    ### 查询处理逻辑

    现在让我们添加用于处理查询与工具调用的核心功能：

    ```csharp
    using var anthropicClient = new AnthropicClient(new APIAuthentication(builder.Configuration["ANTHROPIC_API_KEY"]))
        .Messages
        .AsBuilder()
        .UseFunctionInvocation()
        .Build();

    var options = new ChatOptions
    {
        MaxOutputTokens = 1000,
        ModelId = "claude-3-5-sonnet-20241022",
        Tools = [.. tools]
    };

    Console.ForegroundColor = ConsoleColor.Green;
    Console.WriteLine("MCP 客户端已启动！");
    Console.ResetColor();

    PromptForInput();
    while(Console.ReadLine() is string query && !"exit".Equals(query, StringComparison.OrdinalIgnoreCase))
    {
        if (string.IsNullOrWhiteSpace(query))
        {
            PromptForInput();
            continue;
        }

        await foreach (var message in anthropicClient.GetStreamingResponseAsync(query, options))
        {
            Console.Write(message);
        }
        Console.WriteLine();

        PromptForInput();
    }

    static void PromptForInput()
    {
        Console.WriteLine("输入命令（或键入“exit”退出）：");
        Console.ForegroundColor = ConsoleColor.Cyan;
        Console.Write("> ");
        Console.ResetColor();
    }
    ```

    ## 关键组件说明

    ### 1. 客户端初始化

    * 使用 `McpClientFactory.CreateAsync()` 初始化客户端，该方法会配置传输方式并指定用于启动服务器的命令。

    ### 2. 连接服务器

    * 支持 Python、Node.js 和 .NET 服务器。
    * 服务器通过使用参数中指定的命令启动。
    * 配置为使用 STDIO 与服务器通信。
    * 初始化会话和可用的工具。

    ### 3. 查询处理

    * 聊天客户端基于 [Microsoft.Extensions.AI](https://learn.microsoft.com/dotnet/ai/ai-extensions) 实现。
    * 将 `IChatClient` 配置为启用自动调用工具（函数）。
    * 客户端读取用户输入，并将其发送到服务器。
    * 服务器处理请求并返回响应。
    * 此响应会展示给用户。

    ## 运行客户端

    要让你的客户端连接并运行任意 MCP 服务器：

    ```bash
    dotnet run -- path/to/server.csproj # .NET 服务器
    dotnet run -- path/to/server.py # Python 服务器
    dotnet run -- path/to/server.js # Node.js 服务器
    ```

    <Note>
      如果你在服务器快速上手之后继续进行天气教程，你的命令可能如下：`dotnet run -- path/to/QuickstartWeatherServer`。
    </Note>

    MCP 客户端将：

    1. 连接到指定服务器
    2. 列出可用工具
    3. 开始一个交互式聊天会话，在其中你可以：
       * 输入查询
       * 查看工具执行情况
       * 接收来自 Claude 的回复
    4. 完成后请退出会话

    下面是它连接到天气服务器快速入门时应呈现的示例：

    <Frame>
      <img src="/images/quickstart-dotnet-client.png" />
    </Frame>
  </Tab>
</Tabs>

<div id="next-steps">
  ## 下一步
</div>

<CardGroup cols={2}>
  <Card title="示例服务器" icon="grid" href="/zh/examples">
    浏览我们官方的 MCP 服务器与实现示例集
  </Card>

  <Card title="示例客户端" icon="cubes" href="/zh/clients">
    查看支持 MCP 集成的客户端列表
  </Card>
</CardGroup>