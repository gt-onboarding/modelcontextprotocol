---
title: Ping
---

<Info>**Révision du protocole** : 2025-03-26</Info>

Le Protocole de contexte de modèle (MCP) comprend un mécanisme de ping optionnel permettant à l’une ou l’autre des parties de vérifier que l’autre est toujours réactive et que la connexion est active.

<div id="overview">
  ## Aperçu
</div>

La fonctionnalité de ping est implémentée au moyen d’un simple modèle requête-réponse. Le client ou le serveur peut lancer un ping en envoyant une requête `ping`.

<div id="message-format">
  ## Format des messages
</div>

Une requête ping est une requête JSON-RPC standard sans paramètres :

```json
{
  "jsonrpc": "2.0",
  "id": "123",
  "method": "ping"
}
```

<div id="behavior-requirements">
  ## Exigences de comportement
</div>

1. Le récepteur DOIT répondre rapidement avec une réponse vide :

```json
{
  "jsonrpc": "2.0",
  "id": "123",
  "result": {}
}
```

2. Si aucune réponse n’est reçue dans un délai raisonnable, l’expéditeur PEUT :
   - Considérer la connexion comme expirée
   - Mettre fin à la connexion
   - Tenter une reconnexion

<div id="usage-patterns">
  ## Modèles d’usage
</div>

```mermaid
sequenceDiagram
    participant Expéditeur
    participant Récepteur

    Expéditeur->>Récepteur: requête de ping
    Récepteur->>Expéditeur: réponse vide
```

<div id="implementation-considerations">
  ## Considérations de mise en œuvre
</div>

- Les implémentations DEVRAIENT envoyer périodiquement des pings pour vérifier l’état de la connexion
- La fréquence des pings DEVRAIT être configurable
- Les délais d’expiration DEVRAIENT être adaptés à l’environnement réseau
- Il faudrait éviter un ping trop fréquent afin de réduire la surcharge du réseau

<div id="error-handling">
  ## Gestion des erreurs
</div>

- Les délais d’attente **DEVRAIENT** être traités comme des échecs de connexion
- Plusieurs pings échoués **PEUVENT** déclencher une réinitialisation de la connexion
- Les implémentations **DEVRAIENT** consigner les échecs de ping à des fins de diagnostic